// ============================================================
// НАСТРОЙКИ САЙТА — единственный файл, который нужно править.
// ============================================================
window.SITE_CONFIG = {
  // --- Отправка формы заявки (Cloudflare Worker → Telegram) ---
  // Как поднять воркер и получить URL — см. README, раздел «Форма → Telegram».
  // Токен бота и chat_id живут в секретах Cloudflare, НЕ здесь.
  // Вставьте сюда URL воркера вида https://<имя>.<аккаунт>.workers.dev :
  formEndpoint: '',
  // Пока endpoint пустой, форма работает в демо-режиме (показывает успех,
  // но никуда не отправляет) и честно подписана как прототип.

  // --- Прямые контакты (страницы «Связаться» / Contact) ---
  telegram: '@ваш_ник',            // ← ник в Telegram, с @ (в процессе замены)
  email: 'politushin@gmail.com',

  // --- Цены услуг ---
  // Только стартовые «от», точная цена обсуждается — так и подписано на сайте.
  prices: {
    leads: 'от 5 000 ₽',
    payments: 'от 8 000 ₽',
    ai: 'от 7 000 ₽',
    miniapps: 'от 15 000 ₽',
  },
  pricesEn: {
    leads: 'from $500',
    payments: 'from $900',
    ai: 'from $800',
    miniapps: 'from $1,500',
  },

  // --- Реквизиты (подвал) ---
  // ← ПЛЕЙСХОЛДЕРЫ: отчество и ОГРНИП замените на настоящие
  requisites: 'ИНН 502991709786 · ОГРНИП [PLACEHOLDER]',
  requisitesFull: 'ИП Яценко Даниил [ОТЧЕСТВО]',
  requisitesEn: 'Sole proprietor · TIN 502991709786',

  // --- Отзывы (главная страница) ---
  // Секция скрыта, пока массив пуст. Формат:
  // { text: 'Текст отзыва…', name: 'Имя Фамилия', role: 'Должность, компания' }
  testimonials: [],

  // --- «Обо мне» / About ---
  aboutPhoto: '',    // ← путь к фото Даниила, напр. 'photo.jpg' (файл в корень);
                     //    пока пусто — показывается кружок с инициалами
  aboutLines: [],    // ← 1–2 личные строки на русской странице
  aboutLinesEn: [],  // ← то же для английской (иначе скрыто)
};
