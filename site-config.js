// ============================================================
// НАСТРОЙКИ САЙТА — единственный файл, который нужно править.
// ============================================================
window.SITE_CONFIG = {
  // --- Отправка формы заявки ---
  // 1. Зарегистрируйтесь на https://formspree.io (бесплатно, до 50 заявок/мес)
  // 2. Создайте форму: New form → укажите email-получатель (он хранится
  //    у Formspree, НЕ в этом коде)
  // 3. Скопируйте endpoint вида https://formspree.io/f/xxxxxxxx и вставьте сюда:
  formEndpoint: '',
  // Пока endpoint пустой, форма работает в демо-режиме (показывает успех,
  // но никуда не отправляет) и честно подписана как прототип.

  // --- Прямые контакты (страница «Связаться») ---
  telegram: '@ваш_ник',        // ← ваш ник в Telegram, с @
  email: 'you@example.com',    // ← ваш email

  // --- Цены услуг (страница «Услуги») ---
  // Только стартовые «от», точная цена обсуждается — так и подписано на сайте.
  prices: {
    leads: 'от 5 000 ₽',
    payments: 'от 8 000 ₽',
    ai: 'от 7 000 ₽',
    miniapps: 'от 15 000 ₽',
  },

  // --- Реквизиты ИП (подвал сайта) ---
  // ← ПЛЕЙСХОЛДЕР: замените на настоящие ИНН/ОГРНИП
  requisites: 'ИНН 000000000000 · ОГРНИП 000000000000000',

  // --- Отзывы (главная страница) ---
  // Секция скрыта, пока массив пуст. Формат:
  // { text: 'Текст отзыва…', name: 'Имя Фамилия', role: 'Должность, компания' }
  testimonials: [],

  // --- «Обо мне» ---
  aboutPhoto: '',   // ← путь к фото, напр. 'photo.jpg' (положите файл в корень);
                    //    пока пусто — показывается кружок с инициалами
  aboutLines: [],   // ← 1–2 личные строки, напр.:
                    //    ['Живу в Москве, работаю с клиентами по всей России.',
                    //     'Вне кода — велосипед и настольные игры.']
};
